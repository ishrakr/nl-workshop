services:
  retroassembly:
    image: arianrhodsandlot/retroassembly:latest
    container_name: retroassembly
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    restart: unless-stopped

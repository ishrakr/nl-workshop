FROM alpine:3.22

RUN apk add --no-cache novnc x11vnc xauth \
    && ln -sf /usr/share/novnc/vnc.html /usr/share/novnc/index.html

CMD ["sh", "-c", "echo 'Start this image with docker-compose-web-vnc.yml' >&2; exit 1"]
